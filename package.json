{
  "name": "instagram-clone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend start\"",
    "install-all": "npm --prefix backend install && npm --prefix frontend install --legacy-peer-deps"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
